# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository is currently empty and awaiting initial project setup. No source code, build configuration, or dependencies have been committed yet.

## Expected Project

This repository is intended for an iPhone (iOS) application. Once code is added, update this file with:

- **Build commands**: How to build, run, and archive the app (e.g., `xcodebuild`, `fastlane`, or other tooling)
- **Test commands**: How to run unit tests, UI tests, and any single test
- **Lint/format commands**: SwiftLint, SwiftFormat, or other static analysis tools in use
- **Architecture**: The architectural pattern used (MVC, MVVM, TCA, VIPER, etc.) and key conventions
- **Dependency management**: CocoaPods, Swift Package Manager, Carthage, or a combination
- **Key directories**: Purpose of each top-level source directory
- **Environment setup**: Xcode version requirements, simulator targets, any required environment variables or secrets
